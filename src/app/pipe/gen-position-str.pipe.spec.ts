import { GenPositionStrPipe } from './gen-position-str.pipe';

describe('GenPositionStrPipe', () => {
  it('create an instance', () => {
    const pipe = new GenPositionStrPipe();
    expect(pipe).toBeTruthy();
  });
});
